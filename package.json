{
    "private": "true",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "turbo run dev --parallel",
        "build": "turbo run build"
    },
    "devDependencies": {
        "@storybook/addon-postcss": "^2.0.0",
        "turbo": "^1.11.2"
    }
}
